# Eco-genetic criticality documentation

These documents belong to the separate `eco_genetic_criticality` program. They
are retained for reproducibility and continued theory development, but they are
outside the RACH manuscript and package claim boundary.

The executable modules are in `eco_genetic_criticality/`; demonstrations and
tests are in the matching subdirectories under `examples/` and `tests/`.
