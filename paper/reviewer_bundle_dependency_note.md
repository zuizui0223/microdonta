# Reviewer bundle dependency closure

The anonymous reviewer bundle follows canonical public facades into their private compatibility implementations. The private modules are included only so the extracted methods snapshot executes independently; they do not restore retired publication-facing module names or expand the advertised public API.

The closure regression is locked by `tests/test_reviewer_private_dependency_closure.py`.
